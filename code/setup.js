var en_cour, test, main;

function setup() {
    // Creation du canvas 
    createCanvas(1270, 600);
    fill(255);
    stroke(100);
    frameRate(60);
    en_cour = true;
    test = false;
    main = new ball;
}